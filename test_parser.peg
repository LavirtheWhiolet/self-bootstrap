# TODO: Delete before merging.
%%
# encoding: UTF-8

%%

  'a'? test:<'x'...'z'> { puts test } $ /
  x:abc { puts x } $ /
  !'ghi' 'ghj'
where
  abc <- val:<'b'>;

%%

File.open(ARGV[0]) { |io| yy_parse(io) }